import { MoltiplicaPipe } from './moltiplica.pipe';

describe('MoltiplicaPipe', () => {
  it('create an instance', () => {
    const pipe = new MoltiplicaPipe();
    expect(pipe).toBeTruthy();
  });
});
